# Makefile for the modular ISDN driver
#
EXTRA_CFLAGS += -ggdb
#

ifdef MINCLUDES
CFLAGS += -I$(MINCLUDES) -g
endif

CFLAGS += -DMISDNVERSION=\"$(MISDNVERSION)\"

obj-$(CONFIG_MISDN) += mISDN_core.o

# multi objects

mISDN_core-objs := core.o fsm.o socket.o hwchannel.o stack.o layer1.o layer2.o tei.o

